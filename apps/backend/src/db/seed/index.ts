// Phase 1: Seed functionality will be re-enabled as modules are migrated
// import { seedPrompts } from "@backend/db/seed/prompts.seed";

export const seed = async () => {
	console.log("Seeding database...");

	// Phase 3: Re-enable when prompts module is migrated
	// await seedPrompts();

	console.log("Seeding completed successfully (no seeds active in Phase 1)!");
};

